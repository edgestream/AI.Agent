interface:
  display_name: "Dependency Injection"
  short_description: "DI lifetimes, registrations, and seams"
  default_prompt: "Use $dependency-injection for service lifetimes, registration, options, and testability guidance."

dependencies:
  tools:
    - type: "mcp"
      value: "microsoft-learn"
      description: "Microsoft Learn MCP server for official dependency injection documentation"
      transport: "streamable_http"
      url: "https://learn.microsoft.com/api/mcp"
